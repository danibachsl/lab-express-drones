<h2>Available drones</h2><br>

{{#each response}}
    <ul>
        <b><p>🚀{{ this.name }}: </p></b>
        <p>-> Propellers: {{ this.propellers }}</p>
        <p>-> Max Speed: {{ this.maxSpeed }}</p><br>
        <a href="/drones/{{this._id}}/edit">Update</a>
        <form action='/drones/{{id}}/delete' method="POST">
            <button>Delete</button>
        </form>
    </ul>
{{/each}}
